
export interface Embalse {
  codigo: string;          // Assuming CODIGO is a string
  nombre: string;         // Assuming NOMBRE is a string
  embalse: string;        // Assuming EMBALSE is a string
  x: number;              // Assuming X is a number (coordinate)
  y: number;              // Assuming Y is a number (coordinate)
  demarc: string;         // Assuming DEMARC is a string
  cauce: string;          // Assuming CAUCE is a string
  google: string;         // Assuming GOOGLE is a string (URL)
  openstreetmap: string;  // Assuming OPENSTREETMAP is a string (URL)
  wikidata: string;       // Assuming WIKIDATA is a string (URL)
  provincia: string;      // Assuming PROVINCIA is a string
  ccaa: string;           // Assuming CCAA is a string
  tipo: string;           // Assuming TIPO is a string
  cota_coron: number;     // Assuming COTA_CORON is a number
  alt_cimien: number;     // Assuming ALT_CIMIEN is a number
  informe: string;        // Assuming INFORME is a string
}
